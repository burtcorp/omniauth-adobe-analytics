# OmniAuth Adobe Analytics OAuth2 Strategy

Strategy to authenticate with Adobe Analytics via OAuth2 in OmniAuth

## Installation

Add to your `Gemfile`:

```
gem 'omniauth-adobe-analytics'
```

Then `bundle install`
